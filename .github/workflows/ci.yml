name: CI

on:
  push:
    branches: [main]
  pull_request:
  # So a run can be started by hand. GitHub does not backfill the pushes it
  # missed during an incident, which left a run of commits on main that CI had
  # never seen and no way to ask for one short of an empty commit.
  workflow_dispatch:

permissions:
  contents: read

# On a pull request a new push makes the run in progress irrelevant. On main it
# does not: cancelling there means a burst of commits reports the slowest job —
# the race detector — as "cancelled", which the API calls a failed run and the
# README badge shows as red. Twice today that was mistaken for a real failure.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  check:
    name: build, vet, test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          # From go.mod, so the toolchain cannot drift away from what the module
          # declares it needs.
          go-version-file: go.mod

      # Installed rather than assumed: the runner image carries both today, and
      # a hook that silently stops being syntax-checked is worse than a slow job.
      - name: Install zsh and shellcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y zsh shellcheck

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "these files are not gofmt'd:"
            echo "$unformatted"
            exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: shellcheck
        run: shellcheck -s bash internal/shell/bash.sh

      # The hooks are emitted by the binary with a path substituted in, so the
      # thing worth parsing is what `shcr init` actually prints, not the source
      # template.
      - name: Syntax-check the emitted hooks
        run: |
          go build -o /tmp/shcr ./cmd/shcr
          /tmp/shcr init bash > /tmp/hook.bash
          /tmp/shcr init zsh  > /tmp/hook.zsh
          bash -n /tmp/hook.bash
          zsh  -n /tmp/hook.zsh

      - name: go build
        run: go build ./...

      - name: go test
        run: go test ./...

      # What a user actually installs: pure Go, no cgo, trimmed paths. This is
      # the build that would break first if a cgo-only dependency crept in.
      - name: Release build
        run: |
          CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /tmp/shcr-release ./cmd/shcr
          /tmp/shcr-release version

  race:
    name: race detector
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      # Wall-clock assertions skip themselves under -race, so this run is about
      # data races and nothing else.
      - name: go test -race
        run: go test -race ./...
